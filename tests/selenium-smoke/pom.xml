<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>com.fluxion</groupId>
  <artifactId>selenium-smoke</artifactId>
  <version>1.0.0</version>
  <packaging>jar</packaging>

  <name>Fluxion Selenium Smoke Tests</name>
  <description>Fast smoke tests for the Fluxion UI — runs on every CI push.</description>

  <properties>
    <java.version>21</java.version>
    <maven.compiler.source>${java.version}</maven.compiler.source>
    <maven.compiler.target>${java.version}</maven.compiler.target>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

    <!-- dependency versions -->
    <selenium.version>4.27.0</selenium.version>
    <junit.version>5.11.4</junit.version>
    <assertj.version>3.27.3</assertj.version>
    <slf4j.version>2.0.16</slf4j.version>

    <!-- surefire settings -->
    <surefire.version>3.5.2</surefire.version>
    <!-- Allow overriding base URL from env / system property -->
    <selenium.baseUrl>http://localhost:5173</selenium.baseUrl>
    <selenium.apiBaseUrl>http://localhost:5226/api</selenium.apiBaseUrl>
    <selenium.headless>true</selenium.headless>
  </properties>

  <dependencies>
    <!-- Selenium WebDriver (includes Selenium Manager for auto driver download) -->
    <dependency>
      <groupId>org.seleniumhq.selenium</groupId>
      <artifactId>selenium-java</artifactId>
      <version>${selenium.version}</version>
    </dependency>

    <!-- JUnit 5 -->
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <version>${junit.version}</version>
      <scope>test</scope>
    </dependency>

    <!-- AssertJ -->
    <dependency>
      <groupId>org.assertj</groupId>
      <artifactId>assertj-core</artifactId>
      <version>${assertj.version}</version>
      <scope>test</scope>
    </dependency>

    <!-- Logging -->
    <dependency>
      <groupId>org.slf4j</groupId>
      <artifactId>slf4j-simple</artifactId>
      <version>${slf4j.version}</version>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <version>${surefire.version}</version>
        <configuration>
          <!-- System properties forwarded to tests -->
          <systemPropertyVariables>
            <selenium.baseUrl>${selenium.baseUrl}</selenium.baseUrl>
            <selenium.apiBaseUrl>${selenium.apiBaseUrl}</selenium.apiBaseUrl>
            <selenium.headless>${selenium.headless}</selenium.headless>
          </systemPropertyVariables>
          <!-- JUnit 5 tags: run only tests tagged "smoke" by default -->
          <groups>smoke</groups>
          <!-- Surefire report location -->
          <reportsDirectory>${project.basedir}/target/surefire-reports</reportsDirectory>
          <useFile>true</useFile>
          <!-- Parallel execution for speed -->
          <parallel>classes</parallel>
          <threadCount>2</threadCount>
        </configuration>
      </plugin>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.13.0</version>
      </plugin>
    </plugins>
  </build>
</project>
